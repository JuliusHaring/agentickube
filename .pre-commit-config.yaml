# See https://pre-commit.com for more information
repos:
  - repo: local
    hooks:
      - id: task-dev-all
        name: task dev:all
        entry: task dev:all
        language: system
        pass_filenames: false
