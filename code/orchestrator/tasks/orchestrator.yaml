version: '3'

tasks:
  run:
    cmd: PYTHONPATH="code:code/orchestrator/src" watchmedo auto-restart --directory=code/orchestrator/src --directory=code/shared --patterns="*.py" --recursive -- python code/orchestrator/src/main.py
    desc: Dev-run orchestrator with auto-restart

  generate:
    desc: Placeholder (orchestrator has no codegen)
    cmd: "true"
