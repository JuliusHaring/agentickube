# Generated from code/operator/models.py - task operator:generate
apiVersion: ai.juliusharing.com/v1
kind: Orchestrator
metadata:
  name: example-sequence-job
spec:
  image: agentickube-orchestrator:latest
  imagePullPolicy: Never
  description: One-shot research and summarize pipeline
  llm:
    modelName: qwen3:1.7b
    baseUrl: http://ollama:11434/v1
  agents:
  - name: researcher
  - name: summarizer
  strategy:
    type: sequence
    maxRounds: 10
  trigger:
    type: job
    query: Research and summarize latest security advisories
    backoffLimit: 2
    ttlSecondsAfterFinished: 300
  openTelemetry:
    enabled: true
    endpoint: http://otel-collector:4318
    serviceName: my-orchestrator
    samplingRatio: 1.0
