{{- if .Values.ollama.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.ollama.pvc.size | default "7Gi" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  namespace: {{ .Release.Namespace }}
  labels:
    app: ollama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama
  template:
    metadata:
      labels:
        app: ollama
    spec:
      initContainers:
        - name: pull-model
          image: {{ .Values.ollama.image }}
          command:
            - sh
            - -c
            - |
              set -e
              echo "Starting Ollama server in background..."
              ollama serve >/tmp/ollama-serve.log 2>&1 &
              echo "Waiting for server to be ready..."
              for i in $(seq 1 30); do
                if ollama list 2>/dev/null; then
                  echo "Server ready. Pulling model {{ .Values.ollama.model }}..."
                  break
                fi
                if [ "$i" -eq 30 ]; then
                  echo "Timeout waiting for server" >&2
                  exit 1
                fi
                sleep 2
              done
              ollama pull {{ .Values.ollama.model }}
              echo "Model pull complete."
          volumeMounts:
            - name: models
              mountPath: /root/.ollama
      containers:
        - name: ollama
          image: {{ .Values.ollama.image }}
          args: ["serve"]
          ports:
            - name: http
              containerPort: 11434
          env:
            - name: OLLAMA_HOST
              value: "0.0.0.0"
          volumeMounts:
            - name: models
              mountPath: /root/.ollama
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: ollama-models
---
apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: ollama
  ports:
    - name: http
      port: 11434
      targetPort: 11434
{{- end }}
